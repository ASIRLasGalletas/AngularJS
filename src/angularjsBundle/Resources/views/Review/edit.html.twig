{% extends 'angularjsBundle:Review:index.html.twig' %}

{% block navigation %}
    {{ parent() }}
    <li><a href="{{ path('review_edit', { 'id': entity.id }) }}">Edit</a></li>
{% endblock %}

{% block content %}
    <h1>Review edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('review') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
